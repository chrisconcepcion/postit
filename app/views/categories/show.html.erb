<%= render 'shared/header', header: "Viewing Category: " + @category.name %> 

<% @category.posts.each do |post| %>
	<%= render 'posts/post', post: post %>
<% end %>

<%= link_to 'Home', root_path, class: 'btn btn-small' %>
